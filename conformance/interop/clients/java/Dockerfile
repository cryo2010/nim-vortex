# Java interop client (java.net.http, HTTP/2). Run via single-file source
# launch (java Interop.java), so no separate compile step or build tool.
FROM eclipse-temurin:21-alpine
WORKDIR /app
COPY Interop.java .
CMD ["java", "Interop.java"]
